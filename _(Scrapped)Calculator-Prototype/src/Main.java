package src;

public class Main {
    // public static void main(String[] args) {
    // String function = "y = b*x^2 + 4*x - 16";
    // Function_Alpha f = new Function_Alpha(function);

    // System.out.printf("<%s>\nvar: ", f.getFunction());
    // for (int i = 0; i < 4; i++, System.out.printf("%s",
    // i == 1 ? "exp: " : i == 2 ? "mul: " : i == 3 ? "add: " : ""))
    // for (int j = 0; j < f.getListLength(i); j++)
    // System.out.print(f.getOperation(i, j) + (j == f.getListLength(i) - 1 ? '\n' :
    // ", "));

    // System.out.println("\nResult: " + f.calculate(2, 4));
    // System.out.println("\nResult: " + f.calculate(3, 5));

    // System.out.printf("\n<%s>\nvar: ", f.getFunction());
    // for (int i = 0; i < 4; i++, System.out.printf("%s",
    // i == 1 ? "exp: " : i == 2 ? "mul: " : i == 3 ? "add: " : ""))
    // for (int j = 0; j < f.getListLength(i); j++)
    // System.out.print(f.getOperation(i, j) + (j == f.getListLength(i) - 1 ? '\n' :
    // ", "));
    // }
}